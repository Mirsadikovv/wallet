# check

Запусти полную проверку проекта и покажи результат.

Выполни последовательно:
1. `make fmt` — форматирование
2. `make lint` — линтер  
3. `make test` — тесты
4. `make build` — сборка

После каждого шага сообщи статус (✅ / ❌).
Если есть ошибки — покажи их и предложи исправление.
